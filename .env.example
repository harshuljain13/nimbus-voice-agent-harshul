# Copy to .env and fill in. .env is git-ignored — never commit real keys.
OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=AIza...        # Phase 5
# ELEVENLABS_API_KEY=...        # Phase 8+
# EMBEDDING_PROFILE=light       # light (Railway) | rich (local)

# Public deploy: require each visitor to bring their own key (server .env keys are ignored).
# REQUIRE_USER_KEYS=true
